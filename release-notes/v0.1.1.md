# mita v0.1.1

This release renames the desktop UI from Claude Code Haha to `mita` for the update smoke test.

## Changes

- Updates the desktop product name and window title to `mita`.
- Updates the sidebar brand label to `mita`.
- Updates the About page repository link to `LateAutumn02/mita`.
